<aside>
  <nav class="navbar navbar-inverse sidebar navbar-fixed-top" role="navigation">
<div class="nav-side-menu">
    <div class="brand">Admin Panel</div>
    <i class="fa fa-bars fa-2x toggle-btn" data-toggle="collapse" data-target="#menu-content"></i>
        <div class="menu-list">
  
            <ul id="menu-content" class="menu-content collapse out">

                <li>
                 <%= link_to '<i class="fa fa-home fa-lg"></i>Home'.html_safe, authenticated_root_path %>
                </li>

                <!-- Roles  and Users -->
                <% if  (can? :manage, :all) || @current_permissions["Admin::User"] == "manage" || @current_permissions["Admin::Role"] == "manage" || @current_permissions["Admin::Permission"] == "manage" %>
                  <li data-toggle="collapse" data-target="#users" class="collapsed">
                    <a href="#"><i class="fa fa-user fa-lg"></i> Users & Roles <span class="arrow"></span></a>
                  </li>
                  <ul class="sub-menu collapse" id="users">
                    <% if   (can? :manage, :all) || @current_permissions["Admin::User"] == "manage"%>
                      <li><%= link_to '<i class="fa fa-users fa-lg"></i>All Users'.html_safe, admin_user_index_path %></li>
                    <% end %>
                    <% if   (can? :manage, :all) || @current_permissions["Admin::Role"] == "manage"%>
                      <li><%= link_to '<i class="fa fa-list fa-lg"></i>All Roles'.html_safe, admin_roles_path %></li>
                    <% end %>
                    <% if   (can? :manage, :all) || @current_permissions["Admin::Permission"] == "manage"%>
                      <li><%= link_to '<i class="fa fa-list fa-key"></i>All Permissions'.html_safe, admin_permissions_path %></li>
                    <% end %>
                  </ul>
                <% end %>
                <!--- Customizer -->
                <% if   (can? :manage, :all) || @current_permissions["Admin::Customize"] == "manage"%>
                  <li data-toggle="collapse" data-target="#customizer" class="collapsed">
                    <a href="#"><i class="fa fa-cog fa-lg"></i> Customize <span class="arrow"></span></a>
                  </li>  
                  <ul class="sub-menu collapse" id="customizer">
                    <li><%= link_to '<i class="fa fa-cog fa-lg"></i>Customize Logo'.html_safe, admin_user_index_path %></li>
                  </ul>
                <% end %>
                <!--- Events -->
                <% if   (can? :manage, :all) || @current_permissions["Admin::Event"] == "manage"%>
                  <li data-toggle="collapse" data-target="#events" class="collapsed">
                    <a href="#"><i class="fa fa-calendar-o fa-lg"></i> Events <span class="arrow"></span></a>
                  </li>  
                  <ul class="sub-menu collapse" id="events">
                    <li><%= link_to '<i class="fa fa-calendar fa-lg"></i>All Events'.html_safe, admin_events_path %></li>
                  </ul>
                <% end %>
                <!-- Locations -->
                <% if   (can? :manage, :all) || @current_permissions["Admin::Location"] == "manage"%>
                  <li data-toggle="collapse" data-target="#locations" class="collapsed">
                    <a href="#"><i class="fa fa-map-marker fa-lg"></i> Locations <span class="arrow"></span></a>
                  </li>  
                  <ul class="sub-menu collapse" id="locations">
                    <li><%= link_to '<i class="fa fa-map-marker fa-lg"></i>All Locations'.html_safe, admin_location_index_path %></li>
                  </ul>
                <% end %>
                <!-- Kiosk -->
                <% if   (can? :manage, :all) || @current_permissions["Admin::Kiosk"] == "manage"%>
                  <li data-toggle="collapse" data-target="#kiosk" class="collapsed">
                    <a href="#"><i class="fa fa-desktop fa-lg"></i> Kiosk <span class="arrow"></span></a>
                  </li>  
                  <ul class="sub-menu collapse" id="kiosk">
                    <li><%= link_to '<i class="fa fa-desktop fa-lg"></i>All Kiosk'.html_safe, admin_kiosk_index_path %></li>
                  </ul>
                <% end %>
                <!-- API Call Log -->
                <% if  (can? :manage, :all) %>
                  <li><%= link_to '<i class="fa fa-phone fa-lg"></i>API Call Log'.html_safe, admin_log_index_path %></li>
                  <li><%= link_to '<i class="fa fa-sign-out fa-lg"></i>Log Out'.html_safe, destroy_user_session_path , :method => :delete%></li>
                <% end %>
            </ul>
     </div>
</div>
  </nav>
</aside>


<% content_for :scripts do %>
<script type="text/javascript">
  <% content_for :inner_scripts do %>
  <%= javascript_tag do %>

  var win = $(this);      // browser window
var nav = $('.sidebar'); // your navigation bar

function switchNavbar() {
    if (win.width() < 768) { // on mobile
        nav.removeClass('navbar-static-top');
        nav.addClass('navbar-fixed-bottom');
    } else { // on desktop
        nav.removeClass('navbar-fixed-bottom');
        nav.addClass('navbar-static-top');
    }
}

// On first load
$(function() {
    switchNavbar();
});

// When browser resized
$(window).on('resize', function(){
    switchNavbar();
});




  <% end %>
  <% end %>
</script>
<% end %>]